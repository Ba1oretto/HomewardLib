//file:noinspection VulnerableLibrariesLocal
//buildscript {
//    repositories {
//        mavenLocal()
//    }
//
//    dependencies {
//        classpath 'dev.barroit:specialsource:1.0.1'
//    }
//}

plugins {
    id 'java'
    id 'maven-publish'
    id 'java-library'
//    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

group 'dev.barroit'
version '0.1.0-SNAPSHOT'

//apply plugin: 'dev.barroit.specialsource'

repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = 'papermc-repo'
        url = 'https://repo.papermc.io/repository/maven-public/'
    }
    maven {
        name = 'codemc-maven'
        url = 'https://repo.codemc.org/repository/maven-public/'
    }
    maven {
        name = 'codemc-nms'
        url = 'https://repo.codemc.org/repository/nms-local/'
    }
    maven {
        name = 'sonatype'
        url = 'https://oss.sonatype.org/content/groups/public/'
    }
}

//configurations {
//    compileOnlyApi {
//        transitive false
//    }
//
//    nmsApi {
//        extendsFrom compileOnlyApi
//    }
//}

def developmentVersion = '1.18.2-R0.1-SNAPSHOT'
dependencies {
    // paper minecraft
    compileOnly group: 'io.papermc.paper', name: 'paper-api', version: '1.18.2-R0.1-SNAPSHOT'
    // nms mojang mapping
    compileOnly group: 'org.spigotmc', name: 'spigot', version: "${developmentVersion}", classifier: 'remapped-mojang'

    api group: 'org.reflections', name: 'reflections', version: '0.10.2'

    api group: 'de.tr7zw', name: 'item-nbt-api-plugin', version: '2.10.0'

    api group: 'com.google.code.gson', name: 'gson', version: '2.9.0'

    api group: 'me.mattstudios.utils', name: 'matt-framework', version: '1.4.6'

    api group: 'org.apache.commons', name: 'commons-lang3', version: '3.12.0'

    api group: 'dev.triumphteam', name: 'triumph-gui', version: '3.1.2'

//    // v1_18_2
//    nmsApi group: 'org.spigotmc', name: 'spigot', version: '1.18.2-R0.1-SNAPSHOT', classifier: 'remapped-mojang'
//    // v1_19
//    nmsApi group: 'org.spigotmc', name: 'spigot', version: '1.19-R0.1-SNAPSHOT', classifier: 'remapped-mojang'
}

//shadowJar {
//    minimize()
//    archiveBaseName.set project.name
//    archiveClassifier.set ''
//    archiveVersion.set project.version
//    archiveExtension.set 'jar'
//}

// region minecraft plugin generated
def targetJavaVersion = 17
java {
    def javaVersion = JavaVersion.toVersion(targetJavaVersion)
    sourceCompatibility = javaVersion
    targetCompatibility = javaVersion
    if (JavaVersion.current() < javaVersion) {
        toolchain.languageVersion = JavaLanguageVersion.of(targetJavaVersion)
    }
}

tasks.withType(JavaCompile).configureEach {
    if (targetJavaVersion >= 10 || JavaVersion.current().isJava10Compatible()) {
        options.release = targetJavaVersion
    }
}

processResources {
    def props = [version: version]
    inputs.properties props
    filteringCharset 'UTF-8'
    filesMatching('plugin.yml') {
        expand props
    }
}
// endregion

//publishing {
//    repositories {
//        maven {
//            name = "GitHubPackages"
//            url = uri("https://maven.pkg.github.com/ba1oretto/homewardlib")
//            credentials {
//                username = project.getProperties().get("gpr.user")
//                password = project.getProperties().get("gpr.token")
//            }
//        }
//    }
//    publications {
//        gpr(MavenPublication) {
//            groupId = 'dev.barroit'
//            artifactId = 'homewardlib'
//            version = '0.1.0-SNAPSHOT'
//            from(components.java)
//        }
//        shadow(MavenPublication) { publication ->
//            project.shadow.component(publication)
//        }
//    }
//}
//
//apply from: 'publish.gradle'