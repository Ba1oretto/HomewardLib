apply plugin: 'com.github.johnrengelman.shadow'

shadowJar {
    minimize()
    archiveBaseName.set findProperty('server.plugin.name')
    archiveClassifier.set ''
    archiveVersion.set project.version
    archiveExtension.set 'jar'
}